# Build output
/gaze
/gaze-agent
/gaze-devserver
/dist/

# Local module resolution, per the workspace convention. Never commit this:
# it is correct for this working tree only and wrong inside a build container.
/go.work
/go.work.sum
